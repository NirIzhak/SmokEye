import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'

export default function StoreInfo() {
  return (
    <SafeAreaView>
      <Text style={{ textAlign: "center", marginTop: 80, fontSize: 40 }}>StoreInfo</Text>
    </SafeAreaView>
  )
}